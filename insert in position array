#include<stdio.h>
int main()
{
    int i,pos,n;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    scanf("%d",&pos);
    for(i=pos;i<n;i++)
    {
        arr[i]=arr[i+1];
    }
    n--;
    
    for(i=0;i<n;i++)
    {
        printf("%d ",arr[i]);
    }
}
