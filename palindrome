#include<stdio.h>
int main()
{
    int num,reverse=0,n;
    scanf("%d",&num);
    n=num;
    while(num!=0)
    {
        reverse=(reverse*10)+(num%10);
        num/=10;
    }
    if(reverse==n)
    {
        printf("Palindrome");
    }
    else
    {
        printf("Not Palindrome");
    }
}
